import React, {useEffect, useState} from "react";
import type {Cart} from "../../types/Cart";

const AddToCart: React.FC = () => {
    const [carts, setAddToCart] = useState<Cart[]>([]);
    useEffect(() => {

    }, []);
    return(<div>HELLLOO!!!!!!!</div>);
};

export default AddToCart;